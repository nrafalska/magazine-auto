# 🚀 Швидкий старт - 5 хвилин до першого результату

## Крок 1: Підготуйте дані (2 хв)

### 1.1 Фотографії
```
Покладіть фото клієнта в папку:
input/photos/

Приклад:
input/photos/
├── cover_main.jpg       (обкладинка)
├── photo_01.jpg         (розворот 1 - ліворуч)
├── photo_02.jpg         (розворот 1 - праворуч)
├── photo_03.jpg         (розворот 2 - ліворуч)
└── ...
```

### 1.2 CSV файл

**ВАРІАНТ A: Використати приклад (вже готовий)**
```
✅ Файл input/client_brief.csv вже створений
✅ Можна використати як є для тесту
```

**ВАРІАНТ B: Створити свій**
1. Відкрийте `input/client_brief.csv` в Excel/Google Sheets
2. Відредагуйте дані під свій проект
3. Збережіть

---

## Крок 2: Запустіть генерацію (1 хв)

### Windows Command Prompt / PowerShell:

```bash
# Перейдіть в папку проекту
cd шлях\до\magazine-auto

# Генеруйте варіанти
python scripts/1_prepare_variants.py

# ✅ Результат: 5 папок output/variant_1/ ... output/variant_5/
```

---

## Крок 3: Запустіть InDesign (автоматично)

```bash
python scripts/2_run_indesign.py
```

**Що відбувається:**
- Автоматично відкривається InDesign
- Створюється журнал для кожного варіанту
- Експортуються INDD + PDF

⏱️ **Час:** ~2-3 хвилини на варіант (10-15 хв загалом)

---

## Крок 4: Перегляньте результати

```
output/
├── variant_1/
│   ├── magazine_variant_1.indd  ← Відкрийте в InDesign
│   └── magazine_variant_1.pdf   ← Перегляньте
├── variant_2/
│   └── ...
└── ...
```

**Перегляньте PDF всіх варіантів → Виберіть найкращий!**

---

## 🎯 Перший запуск (тест)

Якщо ви тільки встановили систему:

```bash
# 1. Перевірте чи є приклад
dir input\photos

# 2. Якщо папка порожня - покладіть тестові фото
# (будь-які 6-7 фото для тесту)

# 3. Запустіть
python scripts/1_prepare_variants.py
python scripts/2_run_indesign.py

# 4. Перевірте output/
```

---

## ⚠️ Перше що треба зробити

### 1. Перенесіть свої IDML шаблони

```
Скопіюйте з вашого попереднього проекту:

З: aizine_done/indesign/templates/
└── julia_stula.idml
└── vogue_designes_scripts_label.idml

До: magazine-auto/templates/
└── julia_stula.idml
└── vogue_designes_scripts_label.idml
```

### 2. Перевірте Script Labels

Відкрийте шаблони в InDesign і перевірте що є labels:
- `COVER_IMAGE`, `COVER_TITLE`, `COVER_SUB`
- `L_IMG1`, `L_TITLE`, `L_QUOTE`
- `R_IMG1`, `R_NAME`, `R_BIO`, `R_FACTS`

---

## 🎨 Налаштування під себе

### Змінити стиль:

```bash
# Модний (за замовчуванням)
python scripts/1_prepare_variants.py input/client_brief.csv 5 fashion

# Мінімалізм
python scripts/1_prepare_variants.py input/client_brief.csv 5 minimal

# Сімейний
python scripts/1_prepare_variants.py input/client_brief.csv 5 family
```

### Змінити кількість варіантів:

```bash
# 3 варіанти (швидше)
python scripts/1_prepare_variants.py input/client_brief.csv 3

# 7 варіантів (більше вибору)
python scripts/1_prepare_variants.py input/client_brief.csv 7
```

---

## 🐛 Якщо щось не працює

### Python не знайдено
```bash
# Встановіть Python 3.7+
# Завантажте: https://www.python.org/downloads/
```

### InDesign не запускається
```bash
# Відредагуйте scripts/runjsx.vbs
# Змініть версію InDesign (рядок 20-30)
```

### Шаблони не знайдено
```bash
# Перевірте що IDML файли в папці templates/
dir templates\*.idml
```

---

## ✅ Чеклист готовності

- [ ] Python встановлено
- [ ] InDesign встановлено
- [ ] IDML шаблони в `templates/`
- [ ] Фото клієнта в `input/photos/`
- [ ] CSV файл готовий `input/client_brief.csv`
- [ ] Запущено `1_prepare_variants.py` — OK
- [ ] Запущено `2_run_indesign.py` — OK
- [ ] PDF файли в `output/variant_*/` — OK

**Якщо все ✅ → Готово! 🎉**

---

## 📖 Детальна документація

Див. повний `README.md` для:
- Формат CSV файлу
- Як додавати нові шаблони
- Налаштування стилів
- Troubleshooting
- Майбутні покращення

---

**Час на перший результат: ~5-15 хвилин**  
**Питання? Дивись README.md**
