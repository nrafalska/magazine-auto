# 📰 Система автоматичної генерації журналів

Автоматизована система для створення персоналізованих журналів в Adobe InDesign з AI-підтримкою та варіативністю дизайну.

---

## 🎯 Що робить ця система?

**ВХІД:** Фотографії + бриф від клієнта  
**ВИХІД:** 3-5 варіантів готових журналів (INDD + PDF)

**Головна особливість:** Кожен журнал унікальний — різні комбінації шаблонів, різний порядок розворотів, різні варіанти оформлення.

---

## 📂 Структура проекту

```
magazine-auto/
├── input/                          # Вхідні дані
│   ├── client_brief.csv           # Бриф від клієнта (експорт з Google Sheets)
│   └── photos/                    # Фотографії клієнта
│       ├── cover_main.jpg
│       ├── photo_01.jpg
│       └── ...
│
├── templates/                      # InDesign шаблони
│   ├── vogue_designes_scripts_label.idml
│   ├── julia_stula.idml
│   └── ... (інші IDML файли)
│
├── scripts/                        # Автоматизація
│   ├── 1_prepare_variants.py     # Генерує варіанти план.json
│   ├── 2_run_indesign.py         # Запускає InDesign
│   ├── runjsx.vbs                # VBScript лончер (Windows)
│   └── compose.jsx               # ExtendScript для InDesign
│
├── config/                         # Конфігурація
│   └── templates_config.json     # Налаштування шаблонів
│
└── output/                         # Результати (створюється автоматично)
    ├── variant_1/
    │   ├── plan.json
    │   ├── magazine_variant_1.indd
    │   └── magazine_variant_1.pdf
    ├── variant_2/
    │   └── ...
    └── ...
```

---

## 🚀 Швидкий старт

### Крок 1: Підготовка даних

1. **Отримайте фото від клієнта**
   - Покладіть їх в папку `input/photos/`
   - Формат: JPG, PNG
   - Роздільна здатність: мінімум 300 ppi

2. **Заповніть бриф**
   - Відкрийте Google Sheets або Excel
   - Заповніть таблицю (див. формат нижче)
   - Експортуйте в `input/client_brief.csv`

### Крок 2: Генерація варіантів

```bash
python scripts/1_prepare_variants.py
```

**Що відбувається:**
- Читається CSV з брифом
- Створюється 5 різних варіантів `plan.json`
- Кожен варіант має унікальну комбінацію шаблонів

**Результат:** 5 папок `output/variant_1/` ... `output/variant_5/`

### Крок 3: Запуск InDesign

```bash
python scripts/2_run_indesign.py
```

**Що відбувається:**
- Для кожного варіанту запускається InDesign
- Автоматично створюється журнал
- Експортуються INDD + PDF файли

**Результат:** В кожній папці з'являються готові файли!

### Крок 4: Вибір кращого варіанту

1. Перегляньте PDF файли з усіх варіантів
2. Виберіть найкращий
3. Відкрийте INDD файл в InDesign
4. Зробіть фінальні правки (якщо потрібно)
5. Експортуйте остаточний PDF для друку

---

## 📊 Формат даних (client_brief.csv)

### Приклад таблиці:

| type   | position | photo_path                                  | title    | subtitle          | body_text      | quote           | name     |
|--------|----------|---------------------------------------------|----------|-------------------|----------------|-----------------|----------|
| cover  | 1        | input/photos/cover_main.jpg                 | VIKTORIA | The Art of Being  |                |                 |          |
| spread | 2-3      | input/photos/photo_01.jpg\|input/photos/photo_02.jpg | Beauty   | with Purpose      | Short bio here | "Life is art"   | Viktoria |
| spread | 4-5      | input/photos/photo_03.jpg\|input/photos/photo_04.jpg | Style    | Your Energy       | Another text   | "Be yourself"   | Viktoria |

### Пояснення полів:

- **type** — тип сторінки (`cover` або `spread`)
- **position** — позиція (обкладинка = `1`, розвороти = `2-3`, `4-5`, тощо)
- **photo_path** — шлях до фото (для розворотів — 2 фото через `|`)
- **title** — заголовок
- **subtitle** — підзаголовок
- **body_text** — основний текст (біо, опис)
- **quote** — цитата
- **name** — ім'я особи
- **facts** — факти (через `|` якщо їх декілька)

---

## 🎨 Як працює варіативність?

### Варіант 1 (Базовий)
```
Обкладинка: cover_v1
Розворот 1: spread_v1 (фото A + B) 
Розворот 2: spread_v1 (фото C + D)
Розворот 3: spread_v1 (фото E + F)
```

### Варіант 2 (Інші шаблони)
```
Обкладинка: cover_v2
Розворот 1: spread_v2 (фото A + B)
Розворот 2: spread_v3 (фото C + D)
Розворот 3: spread_v1 (фото E + F)
```

### Варіант 3 (Змінений порядок)
```
Обкладинка: cover_v1
Розворот 1: spread_v1 (фото C + D) ← порядок змінений
Розворот 2: spread_v2 (фото A + B) ← порядок змінений
Розворот 3: spread_v3 (фото E + F)
```

**І так далі — різні комбінації!**

---

## 🛠️ Налаштування шаблонів

### Структура IDML шаблонів

Кожен шаблон повинен мати **Script Labels** на фреймах:

**Обкладинка:**
- `COVER_IMAGE` — фото
- `COVER_TITLE` — заголовок
- `COVER_SUB` — підзаголовок

**Розворот (ліва сторінка):**
- `L_IMG1` — фото
- `L_TITLE` — заголовок
- `L_QUOTE` — цитата

**Розворот (права сторінка):**
- `R_IMG1` — фото
- `R_NAME` — ім'я
- `R_BIO` — біографія
- `R_FACTS` — факти

### Як додати Script Label в InDesign:

1. Виберіть фрейм
2. Window → Utilities → Scripts → Script Label
3. Введіть потрібний label (наприклад: `COVER_IMAGE`)
4. Збережіть IDML файл

### Додавання нового шаблону:

1. Створіть новий IDML файл
2. Додайте Script Labels
3. Збережіть в папку `templates/`
4. Додайте опис в `config/templates_config.json`:

```json
{
  "spreads": {
    "fashion": [
      {
        "file": "новий_шаблон.idml",
        "pages": [2, 3],
        "description": "Опис шаблону"
      }
    ]
  }
}
```

Готово! Скрипт автоматично використовуватиме новий шаблон.

---

## 🔧 Налаштування стилів

### Доступні стилі:

- `fashion` — модний журнал
- `minimal` — мінімалізм
- `family` — сімейний журнал
- `18plus` — журнал 18+

### Як змінити стиль:

```bash
# При генерації варіантів
python scripts/1_prepare_variants.py input/client_brief.csv 5 minimal
```

Або відредагуйте в коді `1_prepare_variants.py`:
```python
generator.generate_all_variants(
    brief_csv='input/client_brief.csv',
    num_variants=5,
    style='minimal'  # ← змініть тут
)
```

---

## 🐍 Python залежності

```bash
# Встановлення (якщо потрібно)
pip install --upgrade pip

# Для розробки (опціонально)
pip install pywin32  # Windows COM для InDesign
```

**Система працює з стандартним Python 3.7+** без додаткових бібліотек!

---

## 📝 Робочий процес дизайнера

### 1. Отримання замовлення

- Клієнт надсилає фото (Google Drive)
- Клієнт заповнює бриф (Google Forms)

### 2. Підготовка даних

- Завантажити фото в `input/photos/`
- Заповнити/експортувати CSV
- Перевірити якість фото (мінімум 300 ppi)

### 3. Генерація AI контенту (опціонально)

Якщо клієнт не надав тексти:

```
ChatGPT/Gemini:
"Напиши короткий опис (150 слів) для модного журналу про [тема]"
```

Вставити згенеровані тексти в CSV.

### 4. Автоматична збірка

```bash
python scripts/1_prepare_variants.py
python scripts/2_run_indesign.py
```

⏱️ Очікування: ~5-10 хвилин

### 5. Вибір і фінальні правки

- Переглянути 5 варіантів PDF
- Вибрати найкращий
- Відкрити INDD в InDesign
- Правки: кернінг, переноси, кольори
- Експорт фінального PDF (CMYK, вильоти 2мм)

### 6. Передача клієнту

- Фінальний PDF
- INDD файл (опціонально)
- Превью всіх варіантів

---

## 🎯 Поради для кращих результатів

### Фотографії:

✅ **Добре:**
- Мінімум 2000px по довгій стороні
- 300 ppi для друку
- Гарне освітлення
- Різноманітні ракурси

❌ **Погано:**
- Розмиті фото
- Низька роздільна здатність
- Темні/пересвічені знімки

### Тексти:

✅ **Добре:**
- Короткі (до 150 слів для біо)
- Емоційні цитати
- Чітка структура

❌ **Погано:**
- Надто довгі тексти
- Складні формулювання
- Багато технічних термінів

### Композиція:

✅ **Добре:**
- Почергування портретів і загальних планів
- Баланс кольорів між розворотами
- Логічна послідовність історії

---

## 🔍 Troubleshooting

### Помилка: "InDesign не запускається"

**Рішення:**
1. Перевірте що InDesign встановлено
2. Відредагуйте `runjsx.vbs` — змініть версію:
   ```vb
   Set objInDesign = CreateObject("InDesign.Application.2023")
   ```

### Помилка: "Шаблон не знайдено"

**Рішення:**
1. Перевірте що IDML файли в папці `templates/`
2. Перевірте `templates_config.json` — правильні назви файлів
3. Перевірте регістр літер (важливо!)

### Помилка: "Фото не вставляється"

**Рішення:**
1. Перевірте шляхи в CSV (мають починатись з `input/photos/`)
2. Перевірте що файли існують
3. Перевірте що у фреймі є Script Label

### Варіанти однакові

**Рішення:**
1. Додайте більше шаблонів в `templates/`
2. Додайте їх в `templates_config.json`
3. Скрипт автоматично використає всі доступні варіанти

---

## 🚧 Майбутні покращення

### Фаза 2: Повна AI інтеграція

- ✅ Автоматична генерація текстів через Gemini API
- ✅ Автоматичний аналіз фото (якість, композиція)
- ✅ Підбір найкращих фото для обкладинки
- ✅ AI-рекомендації по стилю

### Фаза 3: Web інтерфейс

- ✅ Сайт для замовлень
- ✅ Клієнт завантажує фото сам
- ✅ Автоматичне створення CSV з форми
- ✅ Онлайн превью варіантів

### Фаза 4: Друк інтеграція

- ✅ Автоматичне замовлення друку
- ✅ Трекінг статусу
- ✅ Доставка клієнту

---

## 📄 Ліцензія

Приватний проект для внутрішнього використання.

---

## 🆘 Підтримка

Питання? Проблеми?

1. Перевірте цей README
2. Перегляньте лог файли в `output/variant_*/`
3. Перевірте Console в InDesign (Window → Utilities → ExtendScript Toolkit)

---

## ✨ Дякуємо за використання!

**Створено для дизайнерів, які цінують свій час** ⏰  
**Автоматизація рутини = більше часу на творчість** 🎨

---

**Версія:** 1.0  
**Дата:** 2025-01-14
